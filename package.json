{
  "name": "koreandemocrats",
  "version": "0.0.2",
  "scripts": {
    "start": "node_modules/.bin/browser-sync start --server src --files src"
  },
  "dependencies": {
    "browser-sync": "^2.18.8",
    "rsync": "^0.6.1"
  }
}
